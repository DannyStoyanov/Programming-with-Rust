#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
mod some_code {
    /*
    fn identity<T>(value: T) -> T{
        value
    }
    
    struct Point<T> {
        x: T,
        y: T,
    }
    
    impl<T> Point<T> {
        fn x(&self) -> &T {
            &self.x
        }
        fn y(&self) -> &T {
            &self.y
        }
    }
    
    
    // Only Point<f32> will have this interface
    impl Point<f32> {
        fn distance_from_origin(&self) -> f32 {
            (self.x.powi(2) + self.y.powi(2)).sqrt()
        }
    }
    println!("---------------main()---------------");
    let A = Point { x: 4.5 ,y: 3.9};
    println!("A.x:{:?}\nA.y:{:?}", A.x(), A.y());
    println!("{:?}", identity("Identity function testing")); 
    */
}